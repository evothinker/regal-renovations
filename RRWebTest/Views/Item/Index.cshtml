@model List<RRWebTest.Models.ItemViewModel>

<ul>
    @foreach (var item in Model)
    {
        <li>
            @item.Name  @item.Manufacturer  @item.Quantity  @item.Price
        </li>
    }


</ul>




@*<div class="row">
    <div class="col-10">
        <div class="card">
            <div class="card-header">
                <h5>Create New Item</h5>
                
            </div>
            <div class="card-body">
            <form asp-controller="ItemController" asp-action="CreateItem" method="post">
                <div class="mb-3">
                    <label class="form-label">Item Name</label>
                    <input asp-for=Name type="text" class="form-control"/>
                </div>
                <div class="mb-3">
                    <label class="form-label">Manufacturer Name</label>
                    <input asp-for=Manufacturer type="text" class="form-control"/>
                </div>
                <div class="mb-3">
                    <label class="form-label">Quantity</label>
                    <input asp-for=Quantity type="number" class="form-control"/>
                </div>
                <div class="mb-3">
                    <label class="form-label">Price</label>
                    <input asp-for=Price type="number" class="form-control"/>
                </div>
                <div class="mb-3">
                    <button type="submit" class="btn btn-primary">Save</button>
                   
                </div>

            </form>

            </div>
            <div class="card-footer"></div>
        </div>
    </div>
</div>*@